doctype html
html(lang="en")
	- const { title, version } = htmlWebpackPlugin.options
	head
		meta(charset='utf-8')
		meta(http-equiv='X-UA-Compatible', content='IE=edge')
		meta(name='viewport', content='width=device-width,initial-scale=1.0')
		link(rel='icon', href=BASE_URL + 'favicon.ico')
		title= `${title} (v${version})`
	body
		noscript
			strong= `We're sorry but ${title} doesn't work properly without JavaScript enabled. Please enable it to continue.`
		main#app
		//- built files will be auto injected
